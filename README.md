# DevConnect
DevConnect is a simple developer platform backend built using Node.js, Express, and MongoDB. It allows users to register, login using JWT authentication, and create, view, update, and delete posts. The project follows a basic MVC structure and demonstrates core backend concepts like REST APIs, middleware, and database integration.
